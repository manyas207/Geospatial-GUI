"""Overall accuracy from confusion matrix."""

from typing import Any


def compute(predictions: Any, reference: Any) -> float | None:
    # TODO: confusion matrix → diagonal sum / total
    _ = (predictions, reference)
    return None
